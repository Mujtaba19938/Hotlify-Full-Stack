# Copy this file to .env and fill in real values. .env is gitignored.

# Stripe secret key (test mode), used by controller/orderController.js
STRIPE_SECRET_KEY=sk_test_your_key_here

# MongoDB connection string (optional; config/db.js defaults to the local URI below)
MONGO_URI=mongodb://localhost:27017/ROOM-MGMT
